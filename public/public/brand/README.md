# Logo North Bike

Archivo activo: `logo.png`

Ruta pública: `/brand/logo.png`  
Componente: `src/components/brand/BrandLogo.tsx`

Si más adelante tienes versión SVG con fondo transparente, reemplaza el archivo y actualiza el `src` en `BrandLogo`.
